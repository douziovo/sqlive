---
phase: 03-frontend-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sqlive-frontend/src/composables/useInlineEdit.ts
  - sqlive-frontend/src/components/DataVisualizer.vue
autonomous: true
requirements: [FRONT-01, FRONT-04]

must_haves:
  truths:
    - "TypeScript compilation passes without `any` casts in useInlineEdit's emit signature -- `emit: EmitFn` with `EmitFn extends (e: string, ...args: any[]) => void` preserves the caller's emit type"
    - "User navigates away from ER diagram tab and back -- nodes maintain their dagre-computed positions instead of collapsing to {x: 0, y: 0}"
  artifacts:
    - path: "sqlive-frontend/src/composables/useInlineEdit.ts"
      provides: "Generic emit type parameter EmitFn extends (e: string, ...args: any[]) => void"
      contains: "EmitFn extends"
    - path: "sqlive-frontend/src/components/DataVisualizer.vue"
      provides: "ER tab div uses v-show, all other tabs use independent v-if directives (no v-else-if chain)"
      contains: "v-show=\"activeTab === 'er'\""
  key_links:
    - from: "useInlineEdit.ts signature"
      to: "TableSection.vue line 424"
      via: "caller passes emit from defineEmits -- TypeScript infers EmitFn automatically"
      pattern: "useInlineEdit\\(props\\.table\\.name"
    - from: "DataVisualizer.vue ER tab div"
      to: "ErDiagram.vue"
      via: "v-show keeps component mounted; vue-flow internal state preserves node positions"
      pattern: "v-show=\"activeTab === 'er'\""
---

<objective>
Fix emit type safety in useInlineEdit (FRONT-01) and preserve ER diagram node positions across tab switches (FRONT-04).

Purpose: Two independent frontend reliability fixes. FRONT-01 removes `any` from the emit parameter signature in useInlineEdit by making it generic, so callers retain full type checking on emit calls. FRONT-04 prevents ErDiagram.vue from being destroyed/recreated on tab switch by using `v-show` instead of `v-else-if`, so vue-flow's internal dagre-computed node positions persist naturally.

Output: Modified useInlineEdit.ts (generic emit type) + DataVisualizer.vue (v-if chain restructured for v-show ER tab)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-frontend-reliability/03-CONTEXT.md
@.planning/phases/03-frontend-reliability/03-RESEARCH.md
@sqlive-frontend/src/composables/useInlineEdit.ts
@sqlive-frontend/src/components/DataVisualizer.vue
@sqlive-frontend/src/components/er/ErDiagram.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make useInlineEdit emit parameter generic (FRONT-01, D-01, D-02)</name>
  <files>sqlive-frontend/src/composables/useInlineEdit.ts</files>
  <read_first>
    sqlive-frontend/src/composables/useInlineEdit.ts
    sqlive-frontend/src/components/TableSection.vue (line 424 -- current call site)
  </read_first>
  <action>
    Per D-01: Add a generic type parameter `EmitFn extends (e: string, ...args: any[]) => void` to the function declaration, and use `EmitFn` as the type of the `emit` parameter.

    Current signature (lines 4-8):
      export function useInlineEdit(
        tableName: string,
        columnTypes: Record<string, string>,
        emit: (e: string, ...args: any[]) => void
      )

    New signature:
      export function useInlineEdit<EmitFn extends (e: string, ...args: any[]) => void>(
        tableName: string,
        columnTypes: Record<string, string>,
        emit: EmitFn
      )

    Per D-02: Only modify this file. No changes to callers or other files. The caller `TableSection.vue` passes `emit` from `defineEmits` (line 212), and TypeScript will infer `EmitFn` from that argument -- no caller-side changes needed.

    No other changes to the function body or return value. The `autoResizeGhost` and `handleBlur` functions are unchanged. The `emit('update-cell', ...)` call inside handleBlur (line 33) now has type-checked payloads when the caller's emit type is known.

    Do NOT add or remove imports. The file does not need any new imports for this change.
  </action>
  <verify>
    <automated>cd /c/Users/douzi/IdeaProjects/sqlive/sqlive-frontend && npx vue-tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useInlineEdit.ts line 4 contains `<EmitFn extends (e: string, ...args: any[]) => void>` after the function name
    - useInlineEdit.ts line 7 contains `emit: EmitFn` instead of `emit: (e: string, ...args: any[]) => void`
    - No other changes in the file (function body, return value, other parameters unchanged)
    - `npx vue-tsc -b` passes with zero errors
    - `npm run test` passes (existing tests still work)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restructure DataVisualizer tab chain to use v-show for ER diagram (FRONT-04, D-09, D-10)</name>
  <files>sqlive-frontend/src/components/DataVisualizer.vue</files>
  <read_first>
    sqlive-frontend/src/components/DataVisualizer.vue
    sqlive-frontend/src/components/er/ErDiagram.vue (for reference -- understanding onPaneReady lifecycle)
  </read_first>
  <action>
    Per D-09 and D-10: The ER diagram tab currently uses `v-else-if="activeTab === 'er'"` which causes `ErDiagram.vue` to be destroyed when the user switches to another tab and recreated when they come back, losing dagre-computed positions. Change it to `v-show="activeTab === 'er'"` so the component stays mounted.

    Vue 3 requires that a `v-show` element cannot be in the middle of a `v-if`/`v-else-if` chain. Therefore, all `v-else-if` and `v-else` directives in the tab switch structure must be converted to independent `v-if` directives.

    Make these template changes (all within DataVisualizer.vue, lines 29-200):

    (a) Tables tab (line 30): Keep as `v-if="activeTab === 'tables'"` -- no change.

    (b) ER tab (line 59): Change from `v-else-if="activeTab === 'er'"` to `v-show="activeTab === 'er'"`.

    (c) Indexes tab (line 68): Change from `v-else-if="activeTab === 'indexes'"` to `v-if="activeTab === 'indexes'"`.

    (d) Views tab (line 118): Change from `v-else-if="activeTab === 'views'"` to `v-if="activeTab === 'views'"`.

    (e) Triggers tab (line 153): Change from `v-else-if="activeTab === 'triggers'"` to `v-if="activeTab === 'triggers'"`.

    (f) Results tab (line 191): Change from `v-else` to `v-if="activeTab === 'results'"`.

    Keep all CSS classes, attributes, and element content exactly as-is for each div. Only change the Vue directive on each div element.

    No changes to the `<script setup>` section or styles. No changes to ErDiagram.vue. Per D-10, the `onPaneReady` initial layout logic in ErDiagram.vue is still needed for the first render and should be retained.
  </action>
  <verify>
    <automated>cd /c/Users/douzi/IdeaProjects/sqlive/sqlive-frontend && npx vue-tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - DataVisualizer.vue ER div uses `v-show="activeTab === 'er'"` (not v-else-if)
    - DataVisualizer.vue indexes tab uses `v-if="activeTab === 'indexes'"` (not v-else-if)
    - DataVisualizer.vue views tab uses `v-if="activeTab === 'views'"` (not v-else-if)
    - DataVisualizer.vue triggers tab uses `v-if="activeTab === 'triggers'"` (not v-else-if)
    - DataVisualizer.vue results tab uses `v-if="activeTab === 'results'"` (not v-else)
    - No `v-else-if` or `v-else` directives remain among tab content divs
    - `npx vue-tsc -b` passes -- no compilation errors from broken chain
    - ErDiagram.vue is unchanged (no modifications to this file)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend template -> Vue compiler | v-if/v-else-if chain broken by inserting v-show element in the middle causes Vue compiler error or silent rendering misbehavior |
| Emit function -> caller | TypeScript generic constraint may not match Vue 3 defineEmits emit function type, causing compilation errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-BREAK | Tampering | DataVisualizer.vue template | mitigate | All v-else-if and v-else are converted to independent v-if before the v-show insertion; no orphaned v-else-if remains |
| T-03-TYPE | Information Disclosure | useInlineEdit.ts emit signature | mitigate | Generic constraint `extends (e: string, ...args: any[]) => void` ensures broad compatibility with Vue emit types |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `npx vue-tsc -b --noEmit` passes in sqlive-frontend directory
- `npm run test` passes (all existing frontend tests)
- Manual check (post-commit): Open app, switch to ER tab, verify nodes are laid out correctly, switch to another tab, switch back to ER tab -- nodes should maintain their positions
</verification>

<success_criteria>
- useInlineEdit.ts has generic `EmitFn` type parameter -- no "any" in emit signature
- DataVisualizer.vue has no v-else-if or v-else directives among tab content divs; ER tab uses v-show
- TypeScript compilation passes: `npx vue-tsc -b` exits 0
- All existing frontend tests pass: `npm run test` exits 0
- ErDiagram.vue file unchanged
</success_criteria>

<output>
Create `.planning/phases/03-frontend-reliability/03-01-SUMMARY.md` when done
</output>
