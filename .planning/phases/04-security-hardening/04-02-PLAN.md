---
phase: 04-security-hardening
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "ATTACH DATABASE blocking is verified by tests"
    - "PRAGMA blocking is verified by tests"
    - "Normal SQL regression is verified (blocking does not affect legitimate use)"
    - "Error messages match the exact strings specified in D-09 and D-10"
  artifacts:
    - path: "sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java"
      provides: "Test methods covering ATTACH rejection, PRAGMA rejection, and regression"
      contains: "shouldRejectAttachDatabase|shouldRejectPragmaStatement|shouldAllowNormalStatements"
  key_links:
    - from: "SqlExecutionServiceTest test methods"
      to: "SqlExecutionService.execute()"
      via: "service.execute() calls in test assertions"
      pattern: "service\\.execute\\(.*(?:ATTACH|PRAGMA|SELECT).*\\)"
---

<objective>
Add test coverage for ATTACH DATABASE and PRAGMA blocking in SqlExecutionServiceTest.

Purpose: Verify SEC-01 behavior is correct and does not regress existing functionality. Tests prove that ATTACH DATABASE and PRAGMA statements are rejected with proper error messages before SQLite execution, and that normal SQL statements continue to work.

Output: Test methods added to `SqlExecutionServiceTest.java`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-security-hardening/04-CONTEXT.md
@.planning/phases/04-security-hardening/04-01-PLAN.md

# Test file being modified
@sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java

# Source of truth for error message format
@sqlive-backend/src/main/java/com/douzi/sqlive/dto/SqlResponse.java
@sqlive-backend/src/main/java/com/douzi/sqlive/dto/ErrorPayload.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ATTACH DATABASE blocking test cases</name>

  <files>sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java</files>

  <read_first>
    sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java
    sqlive-backend/src/main/java/com/douzi/sqlive/dto/SqlResponse.java
  </read_first>

  <action>
    Add a new section comment `// ============================================================` with header `//  Security: ATTACH DATABASE blocking (SEC-01)` to the test file, following the existing test section pattern.

    Add the following test methods before the closing brace of the class:

    1. `@Test void shouldRejectAttachDatabase()`:
       - Arrange: `SqlResponse r = service.execute("ATTACH DATABASE '/etc/passwd' AS aux;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: Error message equals `"ATTACH DATABASE is not allowed for security reasons"` per D-09
       - Assert: `r.getError().getLine() == 1` (single statement starting at line 1)

    2. `@Test void shouldRejectAttachShorthand()`:
       - Arrange: `SqlResponse r = service.execute("ATTACH ':memory:' AS mem;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: Error message equals `"ATTACH DATABASE is not allowed for security reasons"`

    3. `@Test void shouldRejectAttachLowercase()`:
       - Arrange: `SqlResponse r = service.execute("attach ':memory:' AS mem;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: Error message contains `"ATTACH"` (case-insensitive regex matching means lowercase also blocked)

    4. `@Test void shouldRejectAttachInScript()`:
       - Arrange: `SqlResponse r = service.execute("CREATE TABLE t (x INTEGER);\nATTACH ':memory:' AS aux;\nSELECT 1;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: `r.getError().getLine() == 2` (ATTACH is on the second line)

    Follow the existing test file patterns: use `assertTrue`/`assertFalse`/`assertEquals`/`assertNotNull` from `org.junit.jupiter.api.Assertions` (already statically imported), use `service.execute(...)` with dbName and reset=true, use dbName string unique to each test (e.g. `"attach_test_attachdb"`, `"attach_test_shorthand"`, `"attach_test_lowercase"`, `"attach_test_script"`).
  </action>

  <acceptance_criteria>
    - Source: Section comment `//  Security: ATTACH DATABASE blocking (SEC-01)` exists between section separators
    - Source: Test method `shouldRejectAttachDatabase()` exists and calls `service.execute("ATTACH DATABASE '/etc/passwd' AS aux;", ...)`
    - Source: Test method `shouldRejectAttachShorthand()` exists and calls `service.execute("ATTACH ':memory:' AS mem;", ...)`
    - Source: Test method `shouldRejectAttachLowercase()` exists and calls `service.execute("attach ':memory:' AS mem;", ...)`
    - Source: Test method `shouldRejectAttachInScript()` exists with multi-line script and asserts line number is 2
    - Build: `./gradlew compileTestJava` succeeds
  </acceptance_criteria>

  <verify>
    <automated>./gradlew test --tests "com.douzi.sqlive.service.SqlExecutionServiceTest.shouldRejectAttach*" 2>&1 | tail -10</automated>
  </verify>

  <done>
    Four ATTACH-related test methods exist and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PRAGMA blocking test cases</name>

  <files>sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java</files>

  <read_first>
    sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java
  </read_first>

  <action>
    Add a new section comment `// ============================================================` with header `//  Security: PRAGMA blocking (SEC-01)` to the test file.

    Add the following test methods:

    1. `@Test void shouldRejectPragmaStatement()`:
       - Arrange: `SqlResponse r = service.execute("PRAGMA database_list;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: Error message equals `"PRAGMA statements are not allowed"` per D-10
       - Assert: `r.getError().getLine() == 1`

    2. `@Test void shouldRejectPragmaWithLeadingWhitespace()`:
       - Arrange: `SqlResponse r = service.execute("  PRAGMA page_count;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: Error message contains `"PRAGMA"`

    3. `@Test void shouldRejectPragmaInScript()`:
       - Arrange: `SqlResponse r = service.execute("CREATE TABLE t (x INTEGER);\nINSERT INTO t VALUES (1);\nPRAGMA table_info('t');\nSELECT * FROM t;", "test", true);`
       - Assert: `assertFalse(r.isSuccess())`
       - Assert: `r.getError().getLine() == 3` (PRAGMA is on the third line)

    4. `@Test void shouldRejectVariousPragmaNames()`:
       - Test both `PRAGMA journal_mode;` and `PRAGMA cache_size;` — use a parameterized approach or two separate arrange-assert blocks within a single test
       - Each should return `success=false`
  </action>

  <acceptance_criteria>
    - Source: Section comment `//  Security: PRAGMA blocking (SEC-01)` exists between section separators
    - Source: Test method `shouldRejectPragmaStatement()` exists with error message assertion matching `"PRAGMA statements are not allowed"`
    - Source: Test method `shouldRejectPragmaWithLeadingWhitespace()` exists
    - Source: Test method `shouldRejectPragmaInScript()` exists with line number assertion
    - Source: Test method `shouldRejectVariousPragmaNames()` exists covering journal_mode and cache_size
    - Build: `./gradlew compileTestJava` succeeds
  </acceptance_criteria>

  <verify>
    <automated>./gradlew test --tests "com.douzi.sqlive.service.SqlExecutionServiceTest.shouldRejectPragma*" 2>&1 | tail -10</automated>
  </verify>

  <done>
    Four PRAGMA-related test methods exist and pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression test cases (normal SQL unaffected)</name>

  <files>sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java</files>

  <read_first>
    sqlive-backend/src/test/java/com/douzi/sqlive/service/SqlExecutionServiceTest.java
  </read_first>

  <action>
    Add a new section comment `// ============================================================` with header `//  Security: Regression — normal SQL unaffected (SEC-01)` to the test file.

    Add the following test method:

    1. `@Test void shouldAllowNormalStatementsDespiteBlockingCheck()`:
       - Run `service.execute("SELECT 1;", "reg_test_normal", true)` and assert `isSuccess()`
       - Run `service.execute("CREATE TABLE demo (id INTEGER);", "reg_test_ddl", true)` and assert `isSuccess()`
       - Run `service.execute("INSERT INTO demo VALUES (1); SELECT * FROM demo;", "reg_test_dml", true)` and assert `isSuccess()`
       - Run `service.execute("  SELECT 2;", "reg_test_ws", true)` and assert `isSuccess()` (leading whitespace should not trigger false positive)

    This regression test validates that the blocking regex patterns do not accidentally match normal SQL keywords. Each call uses a unique dbName to avoid cross-contamination.
  </action>

  <acceptance_criteria>
    - Source: Section comment `//  Security: Regression — normal SQL unaffected (SEC-01)` exists between section separators
    - Source: Test method `shouldAllowNormalStatementsDespiteBlockingCheck()` exists
    - Source: The test covers SELECT, CREATE TABLE, INSERT+SELECT, and whitespace-prefixed SELECT
    - Full test suite: `./gradlew test` passes without regressions
  </acceptance_criteria>

  <verify>
    <automated>./gradlew test 2>&1 | tail -20</automated>
  </verify>

  <done>
    Regression test passes. All existing tests still pass. No false positive blocking of normal SQL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test inputs → SqlExecutionService.execute() | Tests simulate malicious user SQL (ATTACH, PRAGMA) to verify blocking. Tests also verify normal SQL passes through unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Information Disclosure | SqlExecutionService.execute() | mitigate (tested) | Four test methods validate ATTACH DATABASE is rejected in all forms: full syntax, shorthand, lowercase, and within multi-statement scripts. Error line number verified for multi-statement scripts. |
| T-04-02 | Information Disclosure | SqlExecutionService.execute() | mitigate (tested) | Four test methods validate PRAGMA is rejected: standard form, leading whitespace, within multi-statement scripts, and for various PRAGMA names (journal_mode, cache_size). |
| T-04-03 | Information Disclosure | SqlExecutionService.execute() | mitigate (tested) | Regression test validates that SELECT, CREATE TABLE, INSERT, and whitespace-prefixed normal SQL are not blocked. |
| T-04-SC | Tampering | Build dependencies | accept | No new dependencies introduced. Tests use existing JUnit and assertions. |
</threat_model>

<verification>
1. `./gradlew test` — all existing tests plus new ATTACH/PRAGMA/regression tests pass
2. `./gradlew compileTestJava` succeeds
3. Manual review: test assertions match D-09 and D-10 error message strings exactly
</verification>

<success_criteria>
- ATTACH DATABASE blocking verified with 4 test methods (full syntax, shorthand, lowercase, multi-line)
- PRAGMA blocking verified with 4 test methods (standard, whitespace, multi-line, various names)
- Regression verified: normal SQL (SELECT, CREATE, INSERT, leading whitespace) is unaffected
- All existing tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/04-security-hardening/04-02-SUMMARY.md` when done
</output>
